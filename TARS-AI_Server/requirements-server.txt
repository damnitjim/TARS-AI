# TARS-AI Companion Server Dependencies
# macOS/Apple Silicon version — CUDA lines removed, torch installed separately for MPS

# Core
torch>=2.6.0
torchaudio>=2.6.0
python-multipart

# Server framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# STT - Speech to Text (faster-whisper + Silero VAD)
faster-whisper>=1.0.0

# TTS - Text to Speech (Piper)
piper-tts>=1.2.0

# LLM - Language Model (transformers + Qwen)
transformers>=4.51.0
accelerate>=0.27.0

# Vision - Image Captioning (BLIP)
Pillow>=10.0.0

# Apple-Silicon MLX backends (optional, macOS only) — selected via config:
#   [vision] model = mlx-community/...        -> mlx-vlm  (e.g. Qwen2.5-VL-7B-Instruct-4bit)
#   [stt]    whisper_model = mlx-community/... -> mlx-whisper (e.g. whisper-large-v3-turbo)
#   [tts]    engine = kokoro                  -> mlx-audio + misaki (Kokoro-82M)
mlx-vlm>=0.6.3
mlx-whisper>=0.4.3
mlx-audio>=0.4.4
misaki[en]>=0.9.4

# Image Generation (Stable Diffusion via diffusers)
diffusers>=0.27.0

# Embeddings
sentence-transformers>=2.2.0

# QR code generation for tunnel URLs
qrcode[pil]>=7.0

# Quantization (8-bit / 4-bit LLM inference)
bitsandbytes>=0.43.0
